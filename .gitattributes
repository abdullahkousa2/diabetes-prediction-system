# Normalise line endings to LF in the repo; check out as native on each OS
* text=auto

# Notebooks and data — treat as binary to avoid diff noise
*.ipynb binary
*.pkl   binary
*.npy   binary
*.json  text eol=lf

# Images
*.png binary
*.jpg binary
*.pdf binary

# Shell scripts must have LF
*.sh text eol=lf
